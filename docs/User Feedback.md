* Light/Dark mode should not apply to the game board. Instead, the gameboard should have its own light/dark mode setting as part of its theme.

## Gameplay screen
* top and bottom trays should have sizes based on the viewport ratio and resolution (fonts and buttons should scale together so text never goes outside of the buttons)
* not sure what the INTEL settings do (force, strict) but if they relate to the Synapse system they should be hidden by default and only show when SYNAPSE is toggled ON
* The word SYNAPSE shouldn't be user facing. The SYNAPSE button should just say INFO and be at the top right corner of the top tray. HP can be moved to the center (will eventually be replaced by a stylized multi stat display HP/MP/SPARK)
* Remove the SYNAPSE button from the bottom tray
* the zoom control should only have the zoom icon and + - symbols (+ would zoom to 7, - would zoom to 11)
* When toggling SYNAPSE ON
  * also hide the WAIT, HUB and RESET buttons from the bottom tray.
  * rename the bottom tray to INFO instead of SYNAPSE
  * show the SYNAPSE settings (intel + future settings)

## Hub
