import type { ScenarioCollection } from './types';

export const pouncerHookScenarios: ScenarioCollection = {
    id: 'pouncer_hook',
    name: 'Pouncer Hook',
    description: 'Legacy enemy-AI parity coverage retired after the shared generic AI migration.',
    scenarios: []
};
